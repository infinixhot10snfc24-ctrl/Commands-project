repeat task.wait() until getgenv().DeltaCmds and getgenv().DeltaCmds.Cmds
local p=game.Players.LocalPlayer
local rs=game:GetService("RunService")
local cam=workspace.CurrentCamera
local wfConn=nil
local function startWf()
	if wfConn then return end
	local function getRoot(char)
		return char and(char:FindFirstChild("HumanoidRootPart")or char:FindFirstChild("Torso")or char:FindFirstChild("UpperTorso"))
	end
	wfConn=rs.Heartbeat:Connect(function()
		local char=p.Character
		local root=getRoot(char)
		if not root then return end
		local vel=root.Velocity
		root.Velocity=vel*10000+Vector3.new(0,10000,0)
		rs.RenderStepped:Wait()
		if root then root.Velocity=vel end
		rs.RenderStepped:Wait()
		if root then root.Velocity=vel+Vector3.new(0,0.1,0)end
	end)
end
local function stopWf()
	if wfConn then wfConn:Disconnect();wfConn=nil end
end
local saOld=nil
local function startSa()
	if saOld then return end
	local mouse=p:GetMouse()
	local maxDist=1000
	local _saTarget=nil
	local _saLast=0
	local function getTarget()
		local now=tick()
		if now-_saLast<0.05 then return _saTarget end
		_saLast=now
		local t,sd=nil,maxDist
		local my=p.Character
		local mr=my and my:FindFirstChild("HumanoidRootPart")
		if not mr then _saTarget=nil;return end
		for _,v in ipairs(game.Players:GetPlayers())do
			if v~=p and v.Character then
				local h=v.Character:FindFirstChild("Head")
				local hu=v.Character:FindFirstChild("Humanoid")
				local tr=v.Character:FindFirstChild("HumanoidRootPart")
				if h and hu and hu.Health>0 and tr then
					local d=(mr.Position-tr.Position).Magnitude
					if d<sd then t=v;sd=d end
				end
			end
		end
		_saTarget=t
		return t
	end
	saOld=hookmetamethod(game,"__index",newcclosure(function(self,key)
		if self==mouse and(key=="Hit"or key=="Target")then
			local t=getTarget()
			if t then
				local h=t.Character and t.Character:FindFirstChild("Head")
				if h then return key=="Hit"and h.CFrame or h end
			end
		end
		return saOld(self,key)
	end))
end
local tpwConn=nil
local function startTpw(spd)
	if tpwConn then tpwConn:Disconnect() end
	local s=spd or 0.5
	local _tpwHum=nil
	p.CharacterAdded:Connect(function() _tpwHum=nil end)
	tpwConn=rs.Stepped:Connect(function()
		if not _tpwHum or not _tpwHum.Parent then
			local c=p.Character
			_tpwHum=c and c:FindFirstChildOfClass("Humanoid")
		end
		if _tpwHum and _tpwHum.MoveDirection.Magnitude>0 then
			_tpwHum.Parent:TranslateBy(_tpwHum.MoveDirection*s)
		end
	end)
end
local function stopTpw()
	if tpwConn then tpwConn:Disconnect();tpwConn=nil end
end
local vfConn=nil
local vfCharConn=nil
local function stopVf()
	if vfConn then vfConn:Disconnect();vfConn=nil end
	if vfCharConn then vfCharConn:Disconnect();vfCharConn=nil end
	local char=p.Character
	if char then
		local root=char:FindFirstChild("HumanoidRootPart")
		if root then
			local bg=root:FindFirstChild("NA_Gyro")
			local bv=root:FindFirstChild("NA_Velocity")
			if bg then bg:Destroy() end
			if bv then bv:Destroy() end
		end
	end
end
getgenv().DeltaCmds.startVf=function(spd)
	stopVf()
	local speed=spd or 50
	local function doFly(char)
		local root=char:WaitForChild("HumanoidRootPart",5)
		if not root then return end
		local bg=Instance.new("BodyGyro",root)
		bg.Name="NA_Gyro";bg.MaxTorque=Vector3.new(9e9,9e9,9e9);bg.P=9e4
		local bv=Instance.new("BodyVelocity",root)
		bv.Name="NA_Velocity";bv.MaxForce=Vector3.new(9e9,9e9,9e9);bv.Velocity=Vector3.zero
		vfConn=rs.Heartbeat:Connect(function()
			if not char:IsDescendantOf(workspace)or not bg.Parent or not bv.Parent then
				vfConn:Disconnect();vfConn=nil;bg:Destroy();bv:Destroy();return
			end
			local mv=getgenv().DeltaCmds.ctrls:GetMoveVector()
			if mv.Magnitude>0 then
				bv.Velocity=cam.CFrame:VectorToWorldSpace(Vector3.new(mv.X,0,mv.Z))*speed
			else
				bv.Velocity=Vector3.zero
			end
			bg.CFrame=cam.CFrame
		end)
	end
	if p.Character then doFly(p.Character) end
	vfCharConn=p.CharacterAdded:Connect(doFly)
end
getgenv().DeltaCmds.stopVf=stopVf
local function startReach(sz)
	local size=tonumber(sz)or 25
	local char=p.Character
	local tool=char and char:FindFirstChildOfClass("Tool")
		or p.Backpack:FindFirstChildOfClass("Tool")
	if not tool then return end
	local val=Instance.new("Vector3Value",tool)
	val.Name="OGSize3";val.Value=tool.Handle.Size
	local sb=Instance.new("SelectionBox")
	sb.Adornee=tool.Handle;sb.Name="FunTIMES"
	sb.Transparency=1;sb.Parent=tool.Handle
	tool.Handle.Massless=true
	tool.Handle.Size=Vector3.new(size,size,size)
end
local function startStats()
	getgenv().DeltaCmds._showStats=true
end
local newCmds={
	{aliases={"walkfling","wf"},              fn=function() startWf() end},
	{aliases={"unwalkfling","unwf"},          fn=function() stopWf() end},
	{aliases={"silentaim"},            fn=function() startSa() end},
	{aliases={"tpwalkspeed","tpwalk"},        args="[speed]", fn=function(v) startTpw(tonumber(v)) end},
	{aliases={"untpwalkspeed","untpwalk"},    fn=function() stopTpw() end},
	{aliases={"vehiclefly","vfly"},           args="[speed]", fn=function(v) getgenv().DeltaCmds.startVf(tonumber(v)) end,
	 hud="speed",hudDefault=50,hudStart="startVf",hudStop="stopVf",
	 hudOn={"vehiclefly","vfly"},hudOff={"unvehiclefly","unvfly"}},
	{aliases={"unvehiclefly","unvfly"},       fn=function() stopVf() end},
	{aliases={"reach"},                       args="[size]", fn=function(v) startReach(v) end},
	{aliases={"stats"},                       fn=function() startStats() end},
}
local cmds=getgenv().DeltaCmds.Cmds
local insertAt=#cmds
for i,entry in ipairs(cmds)do
	if entry.aliases[1]=="commands"then insertAt=i;break end
end
for i,entry in ipairs(newCmds)do
	table.insert(cmds,insertAt+i-1,entry)
end
local function callCmd(alias,arg)
	for _,e in ipairs(getgenv().DeltaCmds.Cmds)do
		for _,a in ipairs(e.aliases)do
			if a==alias then e.fn(arg);return true end
		end
	end
	return false
end
local CMDS={
	fly=function(arg) callCmd("fly",arg) end,
	unfly=function() callCmd("unfly") end,
	vfly=function(arg) callCmd("vehiclefly",arg) end,
	unvfly=function() callCmd("unvehiclefly") end,
	nc=function() callCmd("noclip") end,
	unnc=function() callCmd("unnoclip") end,
	noclip=function() callCmd("noclip") end,
	unnoclip=function() callCmd("unnoclip") end,
	ws=function(arg) callCmd("walkspeed",arg) end,
	lockws=function(arg) callCmd("lockwalkspeed",arg) end,
	unlockws=function() callCmd("unlockwalkspeed") end,
	loopws=function(arg) callCmd("loopwalkspeed",arg) end,
	unloopws=function() callCmd("unloopwalkspeed") end,
	jp=function(arg) callCmd("jumppower",arg) end,
	lockjp=function(arg) callCmd("lockjumppower",arg) end,
	unlockjp=function() callCmd("unlockjumppower") end,
	tp=function(arg) callCmd("goto",arg) end,
	fov=function(arg) callCmd("fov",arg) end,
	maxzoom=function(arg) callCmd("maxzoom",arg) end,
	rj=function() callCmd("rejoin") end,
	respawn=function() callCmd("respawn") end,
	spin=function(arg) callCmd("spin",arg) end,
	unspin=function() callCmd("unspin") end,
	infjump=function() callCmd("infjump") end,
	uninfjump=function() callCmd("uninfjump") end,
	fb=function() callCmd("fullbright") end,
	unfb=function() callCmd("unfullbright") end,
	fling=function(arg) callCmd("fling",arg) end,
	esp=function(arg) callCmd("esp",arg) end,
	unesp=function() callCmd("unesp") end,
	cpos=function() callCmd("copypos") end,
	ctp=function() callCmd("clicktp") end,
	uctp=function() callCmd("unclicktp") end,
	aafk=function() callCmd("antiafk") end,
	fixcam=function() callCmd("fixcam") end,
	hb=function(arg) callCmd("hitbox",arg) end,
	unhb=function() callCmd("unhitbox") end,
	god=function() callCmd("godmode") end,
	ungod=function() callCmd("ungodmode") end,
	view=function(arg) callCmd("view",arg) end,
	unview=function() callCmd("unview") end,
	fc=function(arg) callCmd("freecam",arg) end,
	unfc=function() callCmd("unfreecam") end,
	nofog=function() callCmd("nofog") end,
	unnofog=function() callCmd("unnofog") end,
	antifling=function() callCmd("antifling") end,
	unantifling=function() callCmd("unantifling") end,
	freeze=function() callCmd("freeze") end,
	unfreeze=function() callCmd("unfreeze") end,
	antikick=function() callCmd("antikick") end,
	unantikick=function() callCmd("unantikick") end,
	shop=function() callCmd("serverhop") end,
	sshop=function() callCmd("smallserverhop") end,
	aimlock=function(arg) callCmd("aimlock",arg) end,
	unaimlock=function() callCmd("unaimlock") end,
	noeffect=function() callCmd("noeffect") end,
	avis=function() callCmd("antiinvisible") end,
	ac=function() callCmd("autoclicker") end,
	unac=function() callCmd("unautoclicker") end,
	wf=function() callCmd("walkfling") end,
	unwf=function() callCmd("unwalkfling") end,
	sa=function() callCmd("silentaim") end,
	tpwalk=function(arg) callCmd("tpwalk",arg) end,
	untpwalk=function() callCmd("untpwalk") end,
	reach=function(arg) callCmd("reach",arg) end,
	stats=function() callCmd("stats") end,
}
local adminActive=false
local adminChatConn=nil
local function startAdmin()
	if adminActive then return end
	adminActive=true
	adminChatConn=p.Chatted:Connect(function(msg)
		if msg:sub(1,1)~="/"then return end
		local parts=msg:sub(2):split(" ")
		local key=parts[1]:lower()
		local arg=parts[2]
		if CMDS[key]then CMDS[key](arg)end
	end)
end
local adminInsertAt=#cmds
for i,e in ipairs(cmds)do
	if e.aliases[1]=="commands"then adminInsertAt=i;break end
end
table.insert(cmds,adminInsertAt,{
	aliases={"admin"},
	fn=function()startAdmin()end,
})
