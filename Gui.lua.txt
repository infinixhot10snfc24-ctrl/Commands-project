local p = game.Players.LocalPlayer
local ts = game:GetService("TweenService")
local uis = game:GetService("UserInputService")
local _C=Color3.fromRGB;local _U=UDim2.new;local _N=Instance.new;local _GB=Enum.Font.GothamBold;local _GS=Enum.Font.GothamSemibold;local _G=Enum.Font.Gotham;local _TX=Enum.TextXAlignment;local _UI=Enum.UserInputType
local MAX_SUGG = 5
local ITEM_H = 28
local ITEM_PAD = 2
local sg = _N("ScreenGui", game.CoreGui)
sg.ResetOnSpawn = false
sg.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
local function makeDraggable(frame, handle)
handle = handle or frame
local dragging, dragStart, startPos
local function update(input)
local delta = input.Position - dragStart
frame.Position = _U(
startPos.X.Scale, startPos.X.Offset + delta.X,
startPos.Y.Scale, startPos.Y.Offset + delta.Y
)
end
handle.InputBegan:Connect(function(input)
if input.UserInputType == _UI.MouseButton1
or input.UserInputType == _UI.Touch then
dragging = true
dragStart = input.Position
startPos = frame.Position
input.Changed:Connect(function()
if input.UserInputState == Enum.UserInputState.End then
dragging = false
end
end)
end
end)
uis.InputChanged:Connect(function(input)
if dragging and (input.UserInputType == _UI.MouseMovement
or input.UserInputType == _UI.Touch) then
update(input)
end
end)
end
local btn = _N("TextButton", sg)
btn.Size = _U(0, 56, 0, 56)
btn.Position = _U(0, 20, 0, 20)
btn.BackgroundTransparency = 1
btn.Text = ""
btn.AutoButtonColor = false
btn.Active = true
btn.ZIndex = 10
local btnImg = _N("ImageLabel", btn)
btnImg.Size = _U(1, 0, 1, 0)
btnImg.BackgroundTransparency = 1
btnImg.Image = "rbxassetid://126039728930663"
btnImg.ZIndex = 10
makeDraggable(btn)
local cmdFrame = _N("Frame", sg)
cmdFrame.Size = _U(0, 0, 0, 38)
cmdFrame.Position = _U(0.5, 0, 0.5, 0)
cmdFrame.AnchorPoint = Vector2.new(0.5, 0.5)
cmdFrame.BackgroundColor3 = _C(13, 13, 18)
cmdFrame.ClipsDescendants = true
cmdFrame.Visible = false
_N("UICorner", cmdFrame).CornerRadius = UDim.new(0, 19)
local s2 = _N("UIStroke", cmdFrame)
s2.Color = _C(0, 120, 200)
s2.Thickness = 1.5
local cmdGrad = _N("UIGradient", cmdFrame)
cmdGrad.Color = ColorSequence.new{
ColorSequenceKeypoint.new(0, _C(20, 20, 30)),
ColorSequenceKeypoint.new(1, _C(10, 10, 18)),
}
cmdGrad.Rotation = 90
local cmdIcon = _N("TextLabel", cmdFrame)
cmdIcon.Size = _U(0, 30, 1, 0)
cmdIcon.Position = _U(0, 8, 0, 0)
cmdIcon.BackgroundTransparency = 1
cmdIcon.Text = "⌘"
cmdIcon.TextColor3 = _C(0, 170, 255)
cmdIcon.Font = _GB
cmdIcon.TextSize = 15
local box = _N("TextBox", cmdFrame)
box.Size = _U(1, -44, 1, 0)
box.Position = _U(0, 34, 0, 0)
box.BackgroundTransparency = 1
box.TextColor3 = _C(220, 220, 220)
box.PlaceholderText = "ketik command..."
box.PlaceholderColor3 = _C(80, 80, 100)
box.Font = _GS
box.TextSize = 13
box.TextXAlignment = _TX.Left
local suggFrame = _N("Frame", sg)
suggFrame.Size = _U(0, 260, 0, 0)
suggFrame.Position = _U(0.5, 0, 0.5, -22)
suggFrame.AnchorPoint = Vector2.new(0.5, 1)
suggFrame.BackgroundColor3 = _C(15, 15, 20)
suggFrame.ClipsDescendants = true
suggFrame.Visible = false
_N("UICorner", suggFrame).CornerRadius = UDim.new(0, 10)
local sSugg = _N("UIStroke", suggFrame)
sSugg.Color = _C(0, 100, 180)
sSugg.Thickness = 1
_N("UIListLayout", suggFrame).Padding = UDim.new(0, ITEM_PAD)
local clickingSugg = false
local suggItems = {}
for i = 1, MAX_SUGG do
local item = _N("TextButton", suggFrame)
item.Size = _U(1, 0, 0, ITEM_H)
item.BackgroundTransparency = 1
item.TextColor3 = _C(0, 170, 255)
item.Font = _GS
item.TextSize = 13
item.TextXAlignment = _TX.Left
item.AutoButtonColor = false
item.Text = ""
item.Visible = false
item.LayoutOrder = i
_N("UIPadding", item).PaddingLeft = UDim.new(0, 12)
item.MouseEnter:Connect(function()
item.BackgroundTransparency = 0.75
item.BackgroundColor3 = _C(0, 80, 180)
end)
item.MouseLeave:Connect(function()
item.BackgroundTransparency = 1
end)
item.MouseButton1Down:Connect(function()
clickingSugg = true
end)
item.MouseButton1Click:Connect(function()
local first = string.match(item.Text, "^([^/]+)")
box.Text = first .. " "
clickingSugg = false
box:CaptureFocus()
end)
suggItems[i] = item
end
local cmdListFrame = _N("Frame", sg)
cmdListFrame.Size = _U(0, 300, 0, 380)
cmdListFrame.Position = _U(0.5, 0, 0.5, 0)
cmdListFrame.AnchorPoint = Vector2.new(0.5, 0.5)
cmdListFrame.BackgroundColor3 = _C(25, 25, 25)
cmdListFrame.Visible = false
cmdListFrame.Active = true
_N("UICorner", cmdListFrame).CornerRadius = UDim.new(0, 8)
local sL = _N("UIStroke", cmdListFrame)
sL.Color = _C(0, 170, 255)
sL.Thickness = 2
local titleBar = _N("Frame", cmdListFrame)
titleBar.Size = _U(1, 0, 0, 40)
titleBar.BackgroundColor3 = _C(20, 20, 20)
_N("UICorner", titleBar).CornerRadius = UDim.new(0, 8)
local tFix = _N("Frame", titleBar)
tFix.Size = _U(1, 0, 0.5, 0)
tFix.Position = _U(0, 0, 0.5, 0)
tFix.BackgroundColor3 = _C(20, 20, 20)
tFix.BorderSizePixel = 0
makeDraggable(cmdListFrame, titleBar)
local titleText = _N("TextLabel", titleBar)
titleText.Size = _U(1, -40, 1, 0)
titleText.Position = _U(0, 15, 0, 0)
titleText.BackgroundTransparency = 1
titleText.Text = "Command List"
titleText.TextColor3 = _C(0, 170, 255)
titleText.Font = _GB
titleText.TextSize = 16
titleText.TextXAlignment = _TX.Left
local closeBtn = _N("TextButton", titleBar)
closeBtn.Size = _U(0, 30, 0, 30)
closeBtn.Position = _U(1, -35, 0, 5)
closeBtn.BackgroundTransparency = 1
closeBtn.Text = "❌"
closeBtn.TextSize = 14
closeBtn.Font = _G
local searchBox = _N("TextBox", cmdListFrame)
searchBox.Size = _U(1, -20, 0, 28)
searchBox.Position = _U(0, 10, 0, 44)
searchBox.BackgroundColor3 = _C(35, 35, 35)
searchBox.BorderSizePixel = 0
searchBox.PlaceholderText = "🔍 cari command..."
searchBox.PlaceholderColor3 = _C(100, 100, 100)
searchBox.Text = ""
searchBox.TextColor3 = _C(220, 220, 220)
searchBox.Font = _GS
searchBox.TextSize = 13
searchBox.ClearTextOnFocus = false
_N("UICorner", searchBox).CornerRadius = UDim.new(0, 6)
local searchPad = _N("UIPadding", searchBox)
searchPad.PaddingLeft = UDim.new(0, 8)
local scroll = _N("ScrollingFrame", cmdListFrame)
scroll.Size = _U(1, -20, 1, -82)
scroll.Position = _U(0, 10, 0, 78)
scroll.BackgroundTransparency = 1
scroll.ScrollBarThickness = 4
scroll.CanvasSize = _U(0, 0, 0, 0)
scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
scroll.ScrollBarImageColor3 = _C(0, 170, 255)
local listLayout = _N("UIListLayout", scroll)
listLayout.Padding = UDim.new(0, 5)
listLayout.SortOrder = Enum.SortOrder.LayoutOrder
closeBtn.MouseButton1Click:Connect(function()
cmdListFrame.Visible = false
end)
local COLOR_ON  = _C(80, 220, 120)
local COLOR_OFF = _C(220, 70, 70)
local function makeHUD(label, defaultSpd, startFn, stopFn, yOff)
local active   = false
local speed    = defaultSpd
local expanded = false
local tw       = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
local frame = _N("Frame", sg)
frame.Size = _U(0, 100, 0, 70)
frame.Position = _U(1, -120, 0, 20 + yOff)
frame.BackgroundColor3 = _C(22, 22, 22)
frame.BorderSizePixel = 0
frame.Active = true
frame.Visible = false
_N("UICorner", frame).CornerRadius = UDim.new(0, 12)
local stroke = _N("UIStroke", frame)
stroke.Color = COLOR_ON
stroke.Thickness = 2
local E = 14
local edgeDefs = {
{_U(1,0,0,E), _U(0,0,0,0)},
{_U(1,0,0,E), _U(0,0,1,-E)},
{_U(0,E,1,0), _U(0,0,0,0)},
{_U(0,E,1,0), _U(1,-E,0,0)},
}
for _, ed in ipairs(edgeDefs) do
local strip = _N("TextButton", frame)
strip.Size = ed[1]
strip.Position = ed[2]
strip.BackgroundTransparency = 1
strip.Text = ""
strip.AutoButtonColor = false
strip.ZIndex = 3
makeDraggable(frame, strip)
end
local speedLabel = _N("TextLabel", frame)
speedLabel.Size = _U(1, -8, 0, 18)
speedLabel.Position = _U(0, 4, 0, 4)
speedLabel.BackgroundTransparency = 1
speedLabel.Text = tostring(speed)
speedLabel.TextColor3 = _C(180, 180, 180)
speedLabel.Font = _GB
speedLabel.TextSize = 13
speedLabel.TextXAlignment = _TX.Right
local nameLabel = _N("TextLabel", frame)
nameLabel.Size = _U(1, -36, 1, 0)
nameLabel.Position = _U(0, 10, 0, 0)
nameLabel.BackgroundTransparency = 1
nameLabel.Text = label
nameLabel.TextColor3 = COLOR_ON
nameLabel.Font = _GB
nameLabel.TextSize = 24
nameLabel.TextXAlignment = _TX.Left
local speedBottom = _N("TextLabel", frame)
speedBottom.Size = _U(1, 0, 0, 18)
speedBottom.Position = _U(0, 0, 1, -20)
speedBottom.BackgroundTransparency = 1
speedBottom.Text = tostring(speed)
speedBottom.TextColor3 = _C(180, 180, 180)
speedBottom.Font = _GB
speedBottom.TextSize = 13
speedBottom.TextXAlignment = _TX.Center
makeDraggable(frame, speedBottom)
local toggleBtn = _N("TextButton", frame)
toggleBtn.Size = _U(0, 33, 0, 33)
toggleBtn.Position = _U(1, -26, 0, 4)
toggleBtn.BackgroundColor3 = _C(45, 45, 45)
toggleBtn.BorderSizePixel = 0
toggleBtn.Text = "+"
toggleBtn.TextColor3 = _C(200, 200, 200)
toggleBtn.Font = _GB
toggleBtn.TextSize = 14
_N("UICorner", toggleBtn).CornerRadius = UDim.new(0, 6)
local inputFrame = _N("Frame", sg)
inputFrame.Size = _U(0, 100, 0, 36)
inputFrame.BackgroundColor3 = _C(22, 22, 22)
inputFrame.BorderSizePixel = 0
inputFrame.Visible = false
inputFrame.Active = true
_N("UICorner", inputFrame).CornerRadius = UDim.new(0, 10)
_N("UIStroke", inputFrame).Color = _C(60, 60, 60)
makeDraggable(inputFrame)
local inputBox = _N("TextBox", inputFrame)
inputBox.Size = _U(1, -16, 1, -10)
inputBox.Position = _U(0, 8, 0, 5)
inputBox.BackgroundColor3 = _C(38, 38, 38)
inputBox.BorderSizePixel = 0
inputBox.Text = tostring(speed)
inputBox.TextColor3 = _C(230, 230, 230)
inputBox.Font = _GB
inputBox.TextSize = 14
inputBox.TextXAlignment = _TX.Center
inputBox.PlaceholderText = "speed"
_N("UICorner", inputBox).CornerRadius = UDim.new(0, 6)
local function updateSpeedLabels()
speedLabel.Text = tostring(speed)
speedBottom.Text = tostring(speed)
inputBox.Text = tostring(speed)
end
local function setExpanded(state)
expanded = state
toggleBtn.Text = state and "-" or "+"
if state then
local absPos = frame.AbsolutePosition
local absSize = frame.AbsoluteSize
inputFrame.Position = _U(0, absPos.X, 0, absPos.Y + absSize.Y + 6)
inputFrame.Visible = true
else
inputFrame.Visible = false
end
end
local function applySpeed(val)
local n = tonumber(val)
if not n or n < 1 then return end
speed = n
speedLabel.Text = tostring(speed)
speedBottom.Text = tostring(speed)
if active then startFn(speed) end
end
local function setActive(state, doShow)
active = state
if doShow then frame.Visible = true end
if not state then
frame.Visible = false
inputFrame.Visible = false
if expanded then expanded = false; toggleBtn.Text = "+" end
stopFn()
return
end
startFn(speed)
end
toggleBtn.MouseButton1Click:Connect(function()
setExpanded(not expanded)
end)
local clickArea = _N("TextButton", frame)
clickArea.Size = _U(1, -37, 1, -8)
clickArea.Position = _U(0, 4, 0, 4)
clickArea.BackgroundTransparency = 1
clickArea.Text = ""
clickArea.ZIndex = 2
clickArea.MouseButton1Click:Connect(function()
active = not active
local col = active and COLOR_ON or COLOR_OFF
ts:Create(stroke, tw, {Color = col}):Play()
ts:Create(nameLabel, tw, {TextColor3 = col}):Play()
if active then startFn(speed) else stopFn() end
if not active and expanded then setExpanded(false) end
end)
inputBox.FocusLost:Connect(function()
applySpeed(inputBox.Text)
if tonumber(inputBox.Text) == nil or tonumber(inputBox.Text) < 1 then
inputBox.Text = tostring(speed)
end
end)
return {
show = function(spd)
if spd then speed = math.max(1, spd); updateSpeedLabels() end
setActive(true, true)
end,
hide = function() setActive(false, false) end,
}
end
local function makeToggleHUD(label, startFn, stopFn, yOff)
local active = false
local tw = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
local frame = _N("Frame", sg)
frame.Size = _U(0, 100, 0, 40)
frame.Position = _U(1, -120, 0, 20 + yOff)
frame.BackgroundColor3 = _C(22, 22, 22)
frame.BorderSizePixel = 0
frame.Active = true
frame.Visible = false
_N("UICorner", frame).CornerRadius = UDim.new(0, 12)
local stroke = _N("UIStroke", frame)
stroke.Color = COLOR_ON
stroke.Thickness = 2
local nameLabel = _N("TextLabel", frame)
nameLabel.Size = _U(1, 0, 1, 0)
nameLabel.BackgroundTransparency = 1
nameLabel.Text = label
nameLabel.TextColor3 = COLOR_ON
nameLabel.Font = _GB
nameLabel.TextSize = 20
nameLabel.TextXAlignment = _TX.Center
local E = 14
local edgeDefs = {
{_U(1,0,0,E), _U(0,0,0,0)},
{_U(1,0,0,E), _U(0,0,1,-E)},
{_U(0,E,1,0), _U(0,0,0,0)},
{_U(0,E,1,0), _U(1,-E,0,0)},
}
for _, ed in ipairs(edgeDefs) do
local strip = _N("TextButton", frame)
strip.Size = ed[1]
strip.Position = ed[2]
strip.BackgroundTransparency = 1
strip.Text = ""
strip.AutoButtonColor = false
strip.ZIndex = 3
makeDraggable(frame, strip)
end
local clickArea = _N("TextButton", frame)
clickArea.Size = _U(1, -8, 1, -8)
clickArea.Position = _U(0, 4, 0, 4)
clickArea.BackgroundTransparency = 1
clickArea.Text = ""
clickArea.ZIndex = 2
clickArea.MouseButton1Click:Connect(function()
active = not active
local col = active and COLOR_ON or COLOR_OFF
ts:Create(stroke, tw, {Color = col}):Play()
ts:Create(nameLabel, tw, {TextColor3 = col}):Play()
if active then startFn() else stopFn() end
end)
return {
show = function()
active = true
frame.Visible = true
stroke.Color = COLOR_ON
nameLabel.TextColor3 = COLOR_ON
startFn()
end,
hide = function()
active = false
frame.Visible = false
stopFn()
end,
}
end
local DC = getgenv().DeltaCmds
local cmds = getgenv().DeltaCmds.Cmds
local hudMap = {}
local hudYOffset = 0
for _, entry in ipairs(cmds) do
if entry.hud then
local alias0 = entry.aliases[1]
local label = entry.hudLabel or (alias0:sub(1,1):upper() .. alias0:sub(2))
local hud
if entry.hud == "speed" then
hud = makeHUD(label, entry.hudDefault or 50,
function(spd) if DC[entry.hudStart] then DC[entry.hudStart](spd) end end,
function() if DC[entry.hudStop] then DC[entry.hudStop]() end end,
hudYOffset
)
hudYOffset += 80
elseif entry.hud == "toggle" then
hud = makeToggleHUD(label,
function() if DC[entry.hudStart] then DC[entry.hudStart]() end end,
function() if DC[entry.hudStop] then DC[entry.hudStop]() end end,
hudYOffset
)
hudYOffset += 50
end
if hud then
if entry.hudOn then
local ons = type(entry.hudOn) == "table" and entry.hudOn or {entry.hudOn}
for _, a in ipairs(ons) do
hudMap[a] = { hud = hud, action = "show", isSpeed = entry.hud == "speed" }
end
end
if entry.hudOff then
local offs = type(entry.hudOff) == "table" and entry.hudOff or {entry.hudOff}
for _, a in ipairs(offs) do
hudMap[a] = { hud = hud, action = "hide" }
end
end
end
end
end
local aliasLookup = {}
local displayLabel = {}
for _, entry in ipairs(cmds) do
local label = table.concat(entry.aliases, "/")
for _, alias in ipairs(entry.aliases) do
aliasLookup[alias] = entry
displayLabel[alias] = label
end
end
local function updateSugg(txt)
if txt == "" then suggFrame.Visible = false; return end
local count = 0
local tl = #txt
local seen = {}
for _, entry in ipairs(cmds) do
for _, alias in ipairs(entry.aliases) do
if string.sub(alias, 1, tl) == txt then
local label = table.concat(entry.aliases, "/")
if not seen[label] then
seen[label] = true
count += 1
suggItems[count].Text = entry.args and (label .. " " .. entry.args) or label
suggItems[count].Visible = true
if count >= MAX_SUGG then break end
end
break
end
end
if count >= MAX_SUGG then break end
end
if count == 0 then suggFrame.Visible = false; return end
for i = count + 1, MAX_SUGG do
suggItems[i].Text = ""
suggItems[i].Visible = false
end
suggFrame.Size = _U(0, 260, 0, count * ITEM_H + (count - 1) * ITEM_PAD)
suggFrame.Visible = true
end
local function populateList(filter)
for _, child in ipairs(scroll:GetChildren()) do
if child:IsA("TextLabel") then child:Destroy() end
end
local i = 1
local seen = {}
for _, entry in ipairs(cmds) do
local label = table.concat(entry.aliases, "/")
if not seen[label] then
seen[label] = true
local show = filter == nil or filter == "" or string.find(label, filter, 1, true)
if show then
local item = _N("TextLabel", scroll)
item.Size = _U(1, -10, 0, 25)
item.BackgroundTransparency = 1
item.Text = i .. ". " .. (entry.args and (label .. " " .. entry.args) or label)
item.TextColor3 = _C(255, 255, 255)
item.Font = _GS
item.TextSize = 14
item.TextXAlignment = _TX.Left
item.LayoutOrder = i
i += 1
end
end
end
end
searchBox:GetPropertyChangedSignal("Text"):Connect(function()
populateList(string.lower(searchBox.Text))
end)
local function runCommand(input)
local args = {}
for word in string.gmatch(input, "%S+") do
args[#args + 1] = word:gsub(",", "")
end
if #args == 0 then return end
local c = args[1]
if c == "commands" or c == "cmds" then
searchBox.Text = ""
populateList(nil)
cmdListFrame.Visible = true
return
end
local h = hudMap[c]
if h then
if h.action == "show" then
h.hud.show(h.isSpeed and tonumber(args[2]) or nil)
else
h.hud.hide()
end
return
end
local entry = aliasLookup[c]
if entry and entry.fn then entry.fn(args[2], args[3], args[4]) end
end
local cmdOpen = false
local function closeCmd(input)
	if not cmdOpen then return end
	cmdOpen = false
	box.Text = ""
	suggFrame.Visible = false
	ts:Create(cmdFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = _U(0, 0, 0, 38)}):Play()
	task.delay(0.25, function()
		cmdFrame.Visible = false
		btn.Visible = true
	end)
	if input and input ~= "" then runCommand(input) end
end
btn.MouseButton1Click:Connect(function()
	if cmdOpen then return end
	cmdOpen = true
	btn.Visible = false
	cmdFrame.Visible = true
	cmdFrame.Size = _U(0, 0, 0, 38)
	ts:Create(cmdFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = _U(0, 260, 0, 38)}):Play()
	task.delay(0.1, function()
		box:CaptureFocus()
	end)
end)
box:GetPropertyChangedSignal("Text"):Connect(function()
local txt = string.lower(box.Text)
if string.find(txt, " ") then
suggFrame.Visible = false
else
updateSugg(txt)
end
end)
box.FocusLost:Connect(function(enter)
if clickingSugg then return end
local input = string.lower(box.Text)
if not enter and input == "" then closeCmd(nil); return end
closeCmd(input)
end)